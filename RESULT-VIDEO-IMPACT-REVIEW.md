# 미당첨 영상 사전 준비 영향 점검 — v32

2026-09-18. 범위: 미당첨/참가상 영상이 하나인 설정에서 동일 재생기를 보존한다. 영상 원본, 추첨 규칙, 운영 데이터 저장 형식은 변경하지 않는다.

## 결론

아래 충돌 지점은 분리 또는 보호 처리했다. 격리된 Chrome 반복 재생·DOM·기본 기능 검사를 기준으로 기존 기능의 회귀는 발견되지 않았다. iPad의 메모리 회수/백그라운드 정책과 실제 운영 영상은 별도 실기 확인이 필요하므로 '모든 기기에서 영향 없음'이나 '무조건 무버퍼'로 해석하지 않는다.

| 영향 지점 | 보호 조치와 확인 |
| --- | --- |
| 결과 이미지 영역의 outerHTML 재생성 | 전용 영상은 해당 영역의 **형제 요소**로 고정한다. 이미지/크롭 렌더링을 바꾸지 않고 같은 영상 노드·소스를 10회 재사용했다. |
| 결과 이탈 시 모든 영상/URL 해제 | 전용 영상만 정리 대상에서 제외하고 정지한다. 상품 팝업과 일반 결과 영상은 기존 정리 경로를 유지한다. |
| 추첨 확률·재고·로그 | 준비 함수는 추첨 함수를 호출하지 않는다. 준비 중 로그 0건, 반복 추첨당 차감/로그 1건을 확인했다. |
| 5초 자동 복귀 | 기존 **결과 진입 기준 5초**를 유지한다. 재생 시작 기준으로 변경하지 않았다. |
| 상품 클릭 팝업·키보드 | 영상 레이어는 포인터를 가로채지 않는다. 기존 rc-img 클릭/키 처리와 팝업 중 복귀 중지, 닫은 후 5초 재시작을 유지한다. |
| 당첨 이미지·소환 연출 | 피규어 당첨은 전용 미당첨 영상을 표시하지 않는다. 당첨 이미지 및 기존 소환 연출 경로를 유지한다. |
| 영상 여러 종류·번들 | 노출 참가상의 하위상품 우선/부모 영상 대체 키를 모아 유일한 키가 있을 때만 적용한다. 여러 키면 기존 결과 재생 경로를 사용한다. |
| 영상 변경·삭제·늦은 읽기 응답 | 키가 달라지면 이전 준비를 취소하고 URL을 해제한다. 삭제 후 늦게 끝난 읽기가 이전 영상을 복원하지 않는지 검사했다. 일반 미디어 읽기에도 화면 세대 검사를 추가했다. |
| 무음·BGM·드래그 사운드 | 미당첨 영상은 기존처럼 항상 muted. 기존 오디오 모듈·원본 파일·1.8배 자동 오픈은 변경하지 않았다. |
| 메인 사전 준비 | 메인 현재/다음 영상 준비를 우선한다. 반복 결과 후 메인으로 돌아올 때 준비된 다음 영상 재생을 확인했다. |
| 비활성 결과 화면 노출·포커스 | 준비용 화면은 불투명 레이어로 가리고 inert/aria-hidden으로 입력·접근성 트리에서 제외한다. 결과 카드/화면 진입 애니메이션은 활성 상태에서만 실행한다. |

## 자원·예외 조건

- 전용 영상 **1개와 Blob URL 1개**를 추가 유지한다. 메인 2개 영상과 함께 보유하므로 iPad 메모리 압박 가능성 자체는 없어지지 않는다. 숨겨진 상태에서 계속 루핑하지 않고 시작 지점 준비 후 정지한다.
- 사전 준비가 끝나기 전 결과에 진입하면 `enter.warm:false`가 기록된다. 화면을 지연 전환하거나 포스터·이전 장면으로 대기를 가리지 않는다. 이 경우 즉시 재생 목표는 미달이다.
- 최초 실행·짧은 간격의 반복·백그라운드 복귀·메모리 회수 후에는 다시 준비가 필요할 수 있다. 실패/대기는 최근 100건의 `window.resultVideoDiagnostics`에 로컬 기록하며 외부 전송하지 않는다.
- 프레임 콜백 시간은 촬영으로 확인한 표시 지연과 같지 않다. Windows Chrome 결과를 iPad 결과로 보고하지 않는다.

## 재현 검사

`node scripts/test-result-video-browser.cjs "resource/videos/final-assets/fallen-cinematic-loop.mp4"`

6,808,674바이트 로컬 후보 영상으로 10회 결과 진입: IDB 읽기 1회, 동일 노드/소스 유지, 결과 진입 중 load/seek/waiting 없음. 준비된 첫 프레임에서 시작하며 메인 복귀, 실제 재고/로그, 팝업과 5초 자동 복귀, 당첨 이미지, 여러 영상 대체 경로, 삭제 중 읽기 취소를 검사했다. 후보와 iPad 등록 파일의 동일성은 아직 확인되지 않았다.

추가 회귀: test-figure, test-figure-dom, test-idle-video-cycle, test-idle-video-browser, test-figure-browser, test-scroll-audio, test-audio-mix, test-figure-sw.

## iPad 통과 기준

1. 버전 figure-v32 확인. 데이터 삭제/재설치 없이 기존 운영 설정을 유지한다.
2. 미당첨 20회 이상 반복: 화면 첫 노출부터 해당 영상이 시작하고 이전 이미지·빈 프레임·시작 직후 멈춤이 없어야 한다.
3. 빠른 드래그/자동 오픈 모두 확인. 결과 5초 복귀, 상품 팝업, 당첨 소환 연출, 사운드가 기존과 같아야 한다.
4. 메인 영상도 다음 영상으로 즉시 복귀해야 한다. 앱이 재시작되거나 장시간 사용 후 느려지면 메모리 영향으로 재검토한다.
5. 백그라운드 복귀와 오프라인에서 반복한다. 최초 실행/준비 미완료는 따로 기록하며 정상 반복 성공과 섞지 않는다.
