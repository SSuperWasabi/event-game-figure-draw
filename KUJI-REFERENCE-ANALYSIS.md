# 기존 쿠지 PWA 분석 및 계승 기준

분석일: 2026-09-06
원본: https://github.com/SSuperWasabi/event-game-gatcha
분석 커밋: a1c264c21a304d5e3a0b83fbb79ece4418277a4f
앱 버전: v31 / 서비스 워커 kuji-v31
로컬 참조: reference-kuji/ (원본 코드 변경 없음)

## 범위와 근거

현재 요청은 기존 앱 파악·검토·숙지 단계다. 신규 앱 구현·배포는 아직 하지 않았다. 실제 코드, PRD v2.1, 작업 일지 및 파일 구성을 확인했다. 브라우저 실행·실기기 오프라인·백업 복원 검증은 이번 분석에서 수행하지 않았다. 작업 일지의 과거 테스트 결과는 기존 개발 기록이며 이번 검증 결과가 아니다.

계승 기준은 루트의 초기 프로토타입 kuji-event-game.html이 아니라 kuji-app/index.html이다. README에는 저장 시 재고 초기화 권고, 백업 이전 불가 등 현재 코드와 다른 예전 설명이 남아 있다. 현재 코드와 최신 작업 일지를 우선한다.

## 구조

- 빌드 도구나 프레임워크 없이 HTML/CSS/일반 JavaScript를 index.html에 통합한 정적 앱.
- manifest.webmanifest: 전체화면, 세로 방향, 상대 start_url/scope, 홈 화면 아이콘.
- sw.js: 앱·폰트·뱃지 등의 사전 캐시와 cache-first 요청 처리.
- assets: BI 로고, 아이콘, 폰트, 등급 뱃지 8종, NPC 및 결과 카드 프레임 3종.
- 백엔드·계정 서버·중앙 재고 DB 없음. 백오피스도 동일 HTML 안에 포함.
- localStorage: kuji.config.v1 / kuji.stock.v1 / kuji.log.v1 / kuji.cooldown.v1.
- IndexedDB kuji-media / media: 업로드 이미지와 오디오·영상. 이미지 data URL, 대형 미디어 ArrayBuffer+MIME 저장 및 Blob 변환 지원.
- 작업 일지상 GitHub Pages 배포 및 iPad Pro 12.9 3세대 운영 이력. 현재 배포 상태는 별도로 확인하지 않았다.

## 사용자 흐름

S1 대기 → S2 IP 선택 → S3 경품 라인업 → S4 뽑기 → S6 결과 및 당첨 연출 → 대기.

- IP 1개면 S2 생략. showLineup=false면 S3 생략.
- 별도 S5 연출 화면은 제거되고 결과 화면으로 통합됨.
- 대기 영상 및 NPC는 복수 등록·진입 시 랜덤 표시, 복수 항목일 때 직전 항목 연속 선택 제외.
- 일반 유휴 복귀 기본 30초, S4 뽑기 전 복귀 기본 5초(0=끔). 추첨 확정 후에는 S4 복귀 타이머 해제.
- 당첨 결과는 경품명·이미지·등급·IP·시간·일일 일련번호 표시. 상위/행운상은 번개·색종이·스페셜 효과음.
- 참가자 신원 확인이나 1인 1회 서버 검증 없음. 현장 직원이 참여 및 실물 지급 통제.

## 추첨·재고 규칙

- 노출된 잔여 재고를 티켓 풀로 펼치고 Math.random으로 1개 선택하는 비복원 방식. 추첨 가능 재고가 있으면 100% 당첨.
- 번들은 하위상품 재고까지 펼쳐 재고 비례로 하위상품 1개 지급. 결과에는 실제 하위상품, 로그에는 번들명과 하위상품명 기록.
- 숨김 상품은 라인업·추첨·참여자용 잔여 집계에서 제외하되 데이터 보존.
- 상위 등급별 쿨다운: IP ID+등급명 기준, 기본 15분, 상품별 재정의 및 0으로 해제 가능. 모든 후보가 쿨다운이면 제한을 무시하고 추첨.
- 따라서 쿨다운 활성화 시 항상 전체 잔여 재고 비례인 것은 아니며, 해당 시점의 추첨 가능 후보 안에서 비례함.
- 행운상은 전체 마지막 티켓이 아니라 각 등급 마지막 1개 소진 시 추가 지급. 번들은 하위 재고 합계 소진 기준.
- drawing 플래그와 버튼 비활성화로 동일 페이지 내 연타 방지. 기기 간/탭 간 동시 추첨 제어는 없음.
- 현장 수정의 핵심 원칙: 설정 저장은 기존 잔여를 보존. 전체 재고 리셋은 재고 탭의 2회 확인 동작으로 분리.

## 디자인 계승 범위

- 딥 퍼플 무대 배경, 골드 CTA, 별가루·글로우·네온 프레임.
- 영문 Gyoza, 한글 CookieRun, Pretendard 폴백. 폰트 파일 동봉.
- 등급별 컬러와 이미지 뱃지, 상위 골드/일반 퍼플 결과 프레임.
- BI 로고와 대기 화면 타이포, IP 카드, 경품 카드, 큰 터치 버튼, 화면 전환 및 당첨 연출.
- CSS 변수로 컬러·폰트·라운드·그림자·그라데이션 정의. 600px 이상에서 태블릿용 확대 스타일 적용.
- 뱃지는 글자가 이미지에 포함되어 있어 등급명 변경 시 코드의 텍스트 변경만으로 충분하지 않을 수 있음.
- 디자인을 새로 정하지 않고 기존 화면 구성과 시각 언어를 기본값으로 삼는다.

## 관리자 기능

대기 화면 좌상단을 3초 내 5회 터치하고 PIN 입력. 기본 PIN은 0000. 서버 인증이 아닌 현장 UI 진입 제한이다.

| 탭 | 구현 기능 |
|---|---|
| IP·상품 | 최대 3 IP, 등급/상품명/초기 수량/이미지, 상위 여부, 등급별 쿨다운, 노출/숨김, 순서 변경, 번들·하위상품, 잔여 즉시 조정, 행운상 |
| 재고 | IP·등급별 잔여와 쿨다운 표시, 전체 재고 리셋 |
| 당첨로그 | 당첨 기록, CSV 내보내기, 로그 삭제 |
| 설정 | 이벤트명/PIN/유휴시간/라인업 표시/음소거, 배너, 대기영상, NPC, BGM 3슬롯, 효과음 3슬롯, 백업·복원, 버전 표시 |

이미지 위치·확대·크롭은 조정값을 저장하여 원본을 유지한다. 대기영상 150MB, NPC 이미지 8MB/영상 60MB, 오디오 30MB 제한이 구현되어 있다. 일반 경품 이미지는 1.5MB 초과 경고만 있고 자동 압축은 없다.

## 백업·운영

- .kuji 바이너리: KUJI 매직 헤더, JSON manifest, 미디어 바이트. 포맷 v2.
- 전체 또는 영상 제외 범위. 영상 제외 복원은 대상 기기의 영상과 영상 NPC 목록을 보존.
- PC는 단일 파일, iOS는 약 60MB 목표로 항목 단위 분할. 단일 항목이 크면 해당 조각이 목표치를 넘을 수 있다.
- 분할 파일은 순서대로 복원하며 1번에서 기존 자료를 정리, 이후 파일은 추가 기록.
- 쿨다운 저장값은 백업 대상에 포함되지 않음.
- 최신 운영 모델: PC에서 설정 마스터 관리 → .kuji 내보내기 → 파일 전달 → iPad에서 복원. 실시간 동기화는 아님.

## 새 앱에 앞서 보완할 구체적 지점

1. **저장소 및 캐시 분리:** 동일 origin의 다른 경로에 새 앱을 두면 localStorage와 IndexedDB 이름이 겹친다. 앱 전용 접두사/DB명/백업 식별자를 결정해야 한다. sw.js의 activate는 현재 캐시 외 모든 이름을 삭제하므로 앱 전용 캐시만 정리하도록 범위를 좁힐 필요가 있다. localhost 부팅 코드의 SW/캐시 정리도 origin 전체에 영향을 준다.
2. **오프라인 프레임:** frame-normal 및 결과 gold/purple 프레임은 실제 파일은 있으나 SW 사전 캐시 목록에 없다. 방문한 화면은 런타임 캐시되지만 첫 오프라인 사용 전에 방문하지 않은 결과 종류의 프레임은 누락 가능하다. ?v=2 요청까지 일치하도록 사전 캐시를 보완해야 한다.
3. **SW 오류 경로:** fetch 처리 마지막 catch는 콜백 범위 밖의 hit를 참조한다. 네트워크 실패 경로에서 ReferenceError 가능성이 코드상 확인된다. 수정 및 오프라인 회귀 확인 필요.
4. **저장 실패 처리:** localStorage 저장 예외를 무시하고 일부 IndexedDB 쓰기 결과도 호출자가 검사하지 않는다. 저장 실패에도 성공 안내나 당첨 진행이 가능하다. 재고·로그의 영속화 실패를 감지하고 사용자 흐름에 반영할 필요가 있다.
5. **재고 복구 경계:** 일반 편집 경로는 인덱스를 함께 조정하지만 reconcileStock은 구조 불일치 시 초기 수량으로 되돌리는 분기가 남아 있다. 신규 스키마/백업 이전 시 기존 재고 보존을 별도로 검증해야 한다.
6. **복원 안정성:** 전체 내용 검증 전에 설정 기록·미디어 삭제를 시작한다. 파일 손상·용량 부족·분할 누락 상황을 대비해 사전 검증과 복원 완료 판정을 보완할 여지가 있다.
7. **새 에셋:** 작업 폴더 PNG 6개 중 약 133MB 캐릭터, 약 59MB 키비주얼이 있다. 현재 NPC 8MB 제한과 태블릿 메모리·백업 운영을 고려해 용도별 리사이즈·압축본이 필요하다. 이번 단계에서 원본은 수정하지 않았다.

## 후속 작업 기준

사용자 요구: 기존 구조·디자인·UX/UI·백오피스 계승, 필요한 기능과 에셋만 조정·보완.
아직 제공되지 않은 내용: 별도로 언급한 신규 HTML 본문, 신규 이벤트 추첨 규칙 및 구체적인 변경 목록. 현재 폴더에는 신규 HTML은 없고 PNG 6개가 있다.
후속 구현 시 이 문서와 v31 원본을 기준으로 변경점을 비교한다. 현장 운영에서 확보한 재고 보존, iOS 오디오/파일 선택/백업 대응을 유지하며 변경 범위에 맞춰 검증한다.
